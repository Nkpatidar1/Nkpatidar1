<%= form_with(model: docter) do |form| %>
  <% if docter.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(docter.errors.count, "error") %> prohibited this docter from being saved:</h2>

      <ul>
        <% docter.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :email %>
    <%= form.text_field :email %>
  </div>

  <div class="field">
    <%= form.label :contect_number %>
    <%= form.number_field :contect_number %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
